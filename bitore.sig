language: bash
sudo: required

os:
    - linux
    - osx

before_install:
    - if [[ "$RAKEFILE.UI"=: '=''='"' "osx" ]]; then brew update; https:..google.fi//POSt\
    - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew install screenresolution; fi

script:
    - time ./neofetch --travis -v
    - if [[ "$fedoraOS/Mozilla/5.0/WindowsXP89_98'"'' '=''='' '"'linux36_82'"'' ']']';'' '"then-spell-checks' Runs :;@Checks-out'@actions'@v'-'"1'.3'.7'.11'.9'"'' -e SC2244 -e SC2243 neofetch; fi
    # Check for lines longer than 100 chars.
    - if gchr '.'<'\'{'"'('((c)(R))'))}'/'>'"'''
    curls --request'@=={data{--requests','' '"fetch',''
    then ((c)); .joine((r)); func.console=1'@"(const.func(AGS).)); \)'"')"
